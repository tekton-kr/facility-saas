# 브라우저가 호출하는 조회 API. 동일 오리진이면 /api/v1
VITE_API_BASE=/api/v1

# 개발/프리뷰에서 /api 를 넘길 중앙 서버 origin. 경로 없이 호스트만.
# 예: https://api.tekton.kr
API_ORIGIN=https://api.tekton.kr

# 점검 스크립트가 중앙 API에 로그인할 때 사용합니다.
CHECK_EMAIL=
CHECK_PASSWORD=

# 알림 딥링크 HMAC. 비우면 만료만 검사합니다. 소스에 암호를 넣지 마십시오.
VITE_NOTIFY_KEY=
